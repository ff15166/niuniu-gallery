"use client";

export default function BottomHero() {
  return (
    <section className="bottom-hero">
      <div className="bottom-hero-overlay" />
      <div className="bottom-hero-content">
        <div className="bottom-hero-camera">📷</div>
        <p className="bottom-hero-quote">
          生活不是等待暴风雨过去，而是学会在雨中起舞
        </p>
        <span className="bottom-hero-sub">记录每一个值得珍藏的瞬间</span>
      </div>
    </section>
  );
}
